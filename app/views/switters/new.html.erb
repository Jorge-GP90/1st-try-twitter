<%= form_with(model: @swiit, local: true) do |form| %>
  <div class="swiit_title">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>
  <div class="swiit_content">
    <%= form.label :content %>
    <%= form.text_field :content %>
  </div>
  <%= form.submit %>
<% end %>